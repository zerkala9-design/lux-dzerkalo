# build output
dist/
*.zip
# dependencies
node_modules/
# astro
.astro/
# logs & env
*.log
.env
.env.production
.DS_Store

# runtime shared-store for kabinet (created on server) — самі дані не комітимо,
# але .htaccess має потрапити в деплой, щоб теку не можна було читати з браузера
public/kabinet/data/*
!public/kabinet/data/.htaccess
